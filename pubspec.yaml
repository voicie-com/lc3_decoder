name: lc3_decoder
description: "flutter plugin for decoding lc3 audio."
version: 0.0.1
homepage:

environment:
  sdk: ^3.10.3
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  ffi: ^2.1.3
  hooks: ^1.0.0
  logging: ^1.2.0
  native_toolchain_c: ^0.17.4

dev_dependencies:

  ffigen: ^13.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0




